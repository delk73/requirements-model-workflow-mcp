# Temperature Monitoring Story

A temperature-monitoring system receives temperature readings.

For each reading, the system determines whether to accept or reject it. Accepted readings are recorded.

When an accepted reading exceeds a configured threshold, the system produces an alert.
